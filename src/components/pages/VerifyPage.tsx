// Legacy route retained for old bookmarks. Check-ins now open from scheduled doses.
import { Navigate } from 'react-router-dom';
export default function VerifyPage() {
  return <Navigate to="/" replace />;
}
