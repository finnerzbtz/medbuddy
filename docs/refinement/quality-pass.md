# Calm, focused app refinement

Current release: PR #2 is merged into main and [TestFlight build 5](../ios/releases/0.1.0-5.md) is verified Testing. It includes the follow-ups below and the reminder-retention fix. Historical build-4/local labels below describe when evidence was collected. Physical-device limits still apply.

Branch: `refine/calm-premium-experience`, based on TestFlight build 3 plus its release notes. The user has now requested a TestFlight release of this refinement and a reusable TestFlight skill. Build 4 passed signing/archive verification, uploaded successfully, completed Apple processing and is verified Testing in the existing Reminduh Beta group. See [release notes](../ios/releases/0.1.0-4.md) for exact source and distribution evidence. No backend deployment is included.

The goal remains a high-quality, engaging but calming app whose core functions work in the iOS simulator. A focused visual pass alone does not prove completion. Existing medication behaviour, default sadness thresholds, native data protection, optional self-care separation, owned assets and disabled purchase/cloud gates stay intact.

## First pass

- Room: four primary care controls; room options and activities expand when requested. Pause stays directly reachable. Keyboard Escape restores focus after closing room options. Daily care is compact and optional.
- Settings: a single divided list replaces separate padded cards. Routines opens directly. Help and privacy remain available without repeated footer copy.
- History: the selected day and records lead; calendar, filtering and summary are available on demand. Cross-year dates remain unambiguous.
- Routines: current plan leads, and new ideas appear on request after setup. The first-create focus fallback handles disappearance of its starter button.
- Shop: selecting food or clothing on mobile opens a focused details sheet. Buying still needs a separate explicit confirmation. Cancel and owned-item use preserve medication/self-care state.
- Music: actual playback, loading, failure and retry are visible. Late loading cannot override pause or mute. Test sound exposes failed recordings and can be stopped while loading or playing. Native snooze text no longer describes browser-only delivery.

## Completed verification

The build 4 preparation gate `npm run check:release` passed, including repository hygiene, Release A contracts, the 97 existing domain checks, native planner/storage contracts, asset validation and a production web build. The isolated release browser runner passed the medication, reminder, routine, compact room, history, mobile shop, bundled-media and radio failure/retry suites. WebKit and Chromium checks include 320px layouts, keyboard/focus, accessibility scans, decoded audio output and medication/self-care isolation. The initial 200% root-font checks left some fixed-pixel text unchanged; the next increment below addresses that limitation explicitly.

The broad medication browser suite reports 17 passing scenarios in `rebuild/generated/qa-mvp/browser-results.json`, including schedule edits, record snapshots, supply, undo, restore, local-midnight rollover, cross-tab refresh and the production offline package. Its offline check covers reload, recording, feeding, shop inventory, friendship and History, with no unexpected remote requests or page errors. Companion verification reports six passing scenarios in `rebuild/generated/qa-companion/companion-results.json`, including all 19 cheat states, walking/pause, feeding persistence, camera/pantry sizing and reduced-motion behaviour. Celebration checks cover actual check-ins, Undo, focus, automatic dismissal and calm feedback alternatives.

Both `scripts/ios/radio-playback-browser-tests.mjs` and `scripts/ios/sound-test-control-browser-tests.mjs` pass in Chromium and WebKit. They force recording failures, verify visible retry/error feedback, cancel or mute during loading, and then play the real decoded recording. Radio output is also checked with an audio analyser; a selected music switch alone does not count as successful playback.

## Native simulator acceptance

The final offline bundle passed five XCTest flows on the dedicated iOS 26.5 QA simulator on 10 September 2026: **5 tests, 0 failures**, in 162.7 seconds. Synthetic records were retained to test in-place persistence; personal simulator installations were not modified.

| Flow                  | Verified result                                                                                                                                                                                                                              |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Medication and supply | Enter a synthetic medication through the native keyboard with 10 mg strength and two tablets; record Taken through the review dialog; retain History after process termination; confirm supply falls from 30 to 29 complete scheduled doses. |
| Reminders and backup  | Open the expandable reminder section; receive a real local notification after leaving the app; retain notification opt-in after relaunch; open the native backup share sheet from Backups & data.                                            |
| Radio                 | Load the bundled recording and show Playing only after a source starts; recover after returning from the Home screen; retain enabled/music choices across process termination.                                                               |
| Voice                 | Complete the recorded Cloud preview's loading phase while playback remains active; stop/replay after foregrounding; retain master mute after relaunch.                                                                                       |
| Sensory garden        | Enter the bonsai from Activities; drag over the touch canvas; pause/resume; return to the room using a visible close control.                                                                                                                |

All 165 files in the tested `dist` package matched `ios/App/App/public`; the native configuration had no development-server URL. Cloud endpoints were explicitly blank for the sync. The native tests use the bundled app, rather than a Vite server.

Local evidence:

- `/tmp/reminduh-native-final.xcresult` and `/tmp/reminduh-native-final.log` contain the final successful native run.
- `/tmp/reminduh-native-final-sync.log` records the offline build and Capacitor sync.
- `/tmp/reminduh-refinement-release-browser.log` records the integrated browser checks.
- `/tmp/reminduh-build4-release-check.log` records the passed local release-preparation gate.
- `/tmp/reminduh-refinement-native-shots/manifest.json` maps 11 exported native screenshots to their tests.

The first expanded native run exposed two test-driver assumptions: iOS exposes the pressed garden control as a Switch, and WKWebView can report a control as hittable while fixed navigation covers it. The tests now use the observed accessibility role, scroll the entire target above navigation, and assert/capture the export button's geometry before opening the share sheet. The original failed run remains in `/tmp/reminduh-native-refinement.xcresult`; the assertions were retained and the final rerun passed.

## Visual review and limits

Native screenshots were reviewed for the room, sound controls, record player, bonsai, celebration and backup controls. The record player and sensory overlay render intact, their controls remain reachable, and the backup action can be fully scrolled above navigation. In build 4, the compact speech bubble can overlap Blobby's lower body while he walks near the front of the room. The local follow-up below moves speech outside the canvas. The native History screenshot captures the celebration, so the unobscured History layout is supported by the browser visual/interaction checks rather than that screenshot.

These results establish the listed simulator flows, not every possible function or device condition. Physical iPhone speaker/headphone output, Silent mode, calls/audio interruptions, Focus behaviour and an actual TestFlight update remain device validation. The build 4 suite opened export without a Files-picker restore round trip; the subsequent native follow-up below now covers that round trip. Native local-file music is covered by the later focused follow-up below; VoiceOver navigation and physical audio interruptions remain separate checks. The follow-up also compares normal and accessibility-large Dynamic Type, without claiming the full category/device range.

Cloud sync, Apple Music and real-money purchases remain intentionally disabled; they are not verified release functions. The TestFlight release separately verified the signed archive, upload, Apple processing and assignment to the existing beta group. The broader quality goal is not proven solely by this focused pass.

## Local follow-up after build 4

This is a separate, unshipped increment on the same refinement branch. The build 4 tag and signed archive are unchanged.

- Home keeps one medication review beside Blobby, before the room. It uses the existing historical-aware check-in helper, shows the date for older gaps, and opens the existing review dialog without automatically recording medication. The duplicate shortcut is removed. The greeting/date use less vertical space.
- Speech sits below the actual canvas. A silent CSS grid reserves the natural maximum height of the offline thought deck, including enlarged text, while only one compact bubble is visible and announced. Automatic rotation cannot move a care button under a finger. The room remains visible in portrait, landscape and close-up.
- The friendship and mood labels retain contrast against the darkest room backdrop.
- Help describes the actual Sound controls and offline beta accurately; the restore preview explains that medication reminders must be enabled again.
- Local-file playback ignores a harmless native foreground blur. Pending starts/resumes are cancelled by backgrounding, pause, mute or clearing the queue; cancellation cannot later enable sound or play a revoked file. Genuine preparation/decode errors remain visible.

Browser checks for the home follow-up pass in Chromium and WebKit: a single review is reachable before the room at 320px; a Monday gap still offers a dated review on Thursday; opening/cancelling preserves medication, records and reminders. All 35 thoughts rotate once before repeating, with stable care-control positions at 320–390px and measured 14→28px quote text. The live WebGL room check passes portrait/landscape/close-up speech clearance, keyboard/focus, medication isolation and automated contrast scans, including the darkest room colour. Evidence: `/tmp/reminduh-home-priority.log`, `/tmp/reminduh-companion-reflow.log` and `/tmp/reminduh-refinement/`.

Expanded native flows now include routine create→rest→pause/resume→explicit Done→relaunch; owned clothing changes, feeding and sand touch/undo/background-resume/exit; and export→Save to Files→restore the same JSON→confirm replacement→relaunch. These use only synthetic QA data. Medication history and supply are preserved, and restore correctly returns reminders to off. This extends simulator coverage; it does not prove a physical-device upgrade, actual speaker output or all assistive-technology behaviour.

The text-size follow-up replaces fixed-pixel font sizes with rem values while preserving their browser default sizes. Navigation, medication cards, care controls, tabs and form actions wrap when the text needs more room. Forty browser cases cover ten main routes at 320px and 390px in both engines; measured examples are heading 28→56px, speech 14→28px and care labels 12→24px. Enlarged check-in dialogs, room controls, focus restoration and the form footer also pass. Results and screenshots are in `rebuild/generated/refinement/typography/`; these are specific reflow checks, not a complete accessibility certification. Native WKWebView opts into [WebKit’s supported system body text style](https://bugs.webkit.org/show_bug.cgi?id=187013); the native category comparison below confirms this path in the packaged app.

The local-file lifecycle regression passes all four browser/native-shaped engine profiles, including real generated WAV decoding/output, first-start and resume cancellation, error/retry, radio/file exclusivity, URL cleanup and no uploads. The native-shaped boundary is a test stub, not proof of AVAudioSession or iPhone speaker behaviour. A separate rapid-seek/queue diagnostic reached real `ended` events and advanced tracks but intermittently stalled on a later WebKit Resume interaction. Subsequent bounded traces with and without actual AudioRuntime-equivalent visibility/gesture wiring passed, with every unlock/resume/play promise settling; the earlier cause remains unattributed. The retained diagnostic is not counted as a fully passing release gate. The later focused native Files follow-up below covers selection and queue-end→restart→background→resume; it does not establish the cause of this separate intermittent browser diagnostic. Traces are in `rebuild/generated/refinement/file-player-*-results.json` and `file-player-unlock-trace-*.json`.

The native normal/accessibility-large comparison passed on the final offline package. The Names label frame grew from 22 to 40 points, the medication label from 17 to 30, and its input from 52 to 76. Native screenshots confirm visible enlargement and usable wrapping. A small fixed food-count badge initially clipped its enlarged numeral; the badge now grows with its text. The strengthened final probe passed with complete control bounds above the fixed navigation, and its screenshot confirms the numeral fits. The QA simulator was restored to its normal `large` category. Evidence: `/tmp/reminduh-native-text-normal.xcresult`, `/tmp/reminduh-native-text-accessibility-final.xcresult`, corresponding logs, and `/tmp/reminduh-native-text-accessibility-final-shots/89C4DE0B-2FE5-4ECD-9A27-F5B0F92DB74F.png`.

The integrated browser runner completed with no failures (`/tmp/reminduh-followup-browser.log`). After the badge correction, the forty-case typography suite and new actual-glyph containment assertion passed again (`/tmp/reminduh-typography-badge.log`). Release A/native persistence and all 97 domain checks passed (`/tmp/reminduh-followup-contracts.log`). The latest offline build and Capacitor sync are recorded in `/tmp/reminduh-native-quality-badge-sync.log`; they retain the disabled cloud/purchase gates. The TestFlight skill was updated with the native Files and actual-text-size checks and passed its validator again.

The eight-flow normal-text native run passed seven of eight flows on the package with the corrected food badge; its routine test failed because the driver scrolled from the dialog edge. A targeted retry reached Home but tapped the persistent “Routine added.” toast covering the activity. The driver now scrolls within the observed dialog, explicitly dismisses that message, verifies stable visible button bounds and taps once. The final routine-only check passed in 84.055 seconds, including pause/resume, explicit Done, medication/supply isolation and relaunch persistence. All eight flows therefore have passing evidence on the same unchanged app package across these runs; this was not a single eight-test/zero-failure run. The failed bundles and video remain available rather than being overwritten. Evidence: `/tmp/reminduh-native-quality-eight.xcresult` (seven passes, one driver failure), `/tmp/reminduh-native-routine-dialog-final.xcresult` (toast obstruction), and `/tmp/reminduh-native-routine-uncovered-final.xcresult` (targeted pass), with corresponding logs and four screenshots listed in `/tmp/reminduh-native-routine-uncovered-final-shots/manifest.json`.

The production offline flow also passed all four checks after packaging (`/tmp/reminduh-followup-offline.log`). No new archive, upload, release tag or backend deployment was made for this local follow-up.

A subsequent screenshot review found that native routine date/time fields extended 28 points past their intended width. Constraining the grid track alone did not fix it; that failed assertion is retained in `/tmp/reminduh-native-routine-fields-normal-executed.xcresult`. The size difference matched padding plus borders and [WebKit’s reported iOS percentage-width issue](https://bugs.webkit.org/show_bug.cgi?id=301648). The scoped fix uses a shrinking grid track and automatic stretched widths for these two input types, preserving their native pickers. Both now measure x31, width340, right371 inside the dialog’s right390. The focused native test passed in 52.514 seconds, opened both pickers, cancelled the form and confirmed medication history/supply were unchanged. Evidence: `/tmp/reminduh-native-routine-width-normal.xcresult` and log; offline packaging in `/tmp/reminduh-native-routine-width-sync.log`. An initial discovery attempt executed zero tests and is explicitly excluded from validation. The nine routine browser checks passed after this final CSS change (`/tmp/reminduh-routine-fields-browser.log`). The earlier eight-flow evidence was not rerun for this scoped form sizing change.

The same focused form test also passed at accessibility-large (89.859 seconds), with both fields still 340 points wide and their height growing from 57 to 87 points. Both native pickers opened, Done populated the time, and cancelling preserved medication data. The QA text category was restored to normal `large`. Evidence: `/tmp/reminduh-native-routine-width-accessibility.xcresult`, its log, and the normal/accessibility `-shots/manifest.json` files.

## Sound, narration and contextual feedback follow-up

These changes are local refinements after build 4; the distributed archive and release tag are unchanged.

Ordinary confirmations remain available while reading the current page and clear on later navigation. Medication feedback with Undo remains across pages until explicitly dismissed. Medication saves and backup restores retain their new confirmation on arrival; this requires an explicit destination because the state update can commit before the router transition. Dismissing a message restores focus to its usable original control or the current page when that control was removed or hidden. The destination is ephemeral toast state only: persisted app data, backup schema, native persistence and cloud protocol are unchanged, so no schema migration is needed.

Sound has a fixed compact header and a scrolling preferences body. Close stays reachable while browsing voices and Test sound; close/Escape return focus without changing audio preferences. Navigating to the record player gives focus to the new page. The offline beta hides the unavailable Apple Music card while preserving local files and Blobby radio. Integration gates remain disabled.

The existing wisdom/voice suites now exercise the single automatic speech bubble and current Sound controls instead of the removed full section. Both engines verify the full 35-thought rotation and five attributed quotes, actual decoded voice output, matching visible text and its timed successor, voice replacement, loading/error/retry, mute/cancellation, hidden/offscreen/dialog pauses and no burst of queued speech on return. Health and self-care data remain unchanged.

Verification on the final app source:

- 12 Chromium/WebKit contextual-message checks: 95-second persistence, actual medication save, backup file chooser/restore, navigation, dose Undo, 200% text, dismissal and focus after a hidden trigger. Evidence: `rebuild/generated/refinement/toast-navigation/results.json` and `/tmp/reminduh-toast-navigation-final-package.log`.
- Eight Sound/record-player cases across 320/390px and normal/200% text in both engines: actual scrolling, close-button geometry and hit testing, settings retention, focus, usable offline sources and the native browser file chooser. Evidence: `rebuild/generated/refinement/sound-dialog/` and `/tmp/reminduh-sound-dialog-check.log`.
- 42 wisdom/voice checks across six engine/suite runs, with no page errors or unexpected remote requests. Evidence: `rebuild/generated/qa-wisdom-current/`.
- `check:release` passed, including Release A/native persistence contracts, 97 domain checks, assets, TypeScript and the offline production build. The expanded release browser runner passed all 21 scripts, now including these message, Sound and narration checks. Evidence: `/tmp/reminduh-navigation-release-check.log` and `/tmp/reminduh-navigation-release-browser.log`.

The new native Files fixture helper writes only two synthetic WAV files, an intentionally invalid audio file and their manifest to the explicitly named dedicated QA simulator's Files folder. It rejects a missing/wrong simulator, a non-booted device, linked targets and differing existing fixture content. It does not erase a device or modify app data. Missing/wrong-device rejection and Python syntax were checked.

The native local-file baseline passed one test with zero failures in 142.213 seconds on the previous offline package, whose file-audio implementation is unchanged. It selected genuine files through Files, observed progressing playback and automatic advancement from an 8-second track to a 14-second track without seeking, restarted after the final end, held pause, resumed after Home/foreground, checked radio/file exclusion, surfaced an invalid WAV error and recovered with a valid file. Original sound preferences and medication/history/supply were retained. Evidence: `/tmp/reminduh-native-file-music-lifecycle.xcresult` and its log. This proves the listed native transport/decode flow, not physical speaker output.

The final offline package then passed both focused native tests with **2 tests, 0 failures in 186.060 seconds**: Files music in 143.106 seconds and Sound/confirmation navigation in 42.954 seconds. The latter kept Close at the same visible position after scrolling preferences, retained sound choices, cleared the names confirmation on Today and preserved profile and medication data. Reviewed screenshots show reachable Sound dismissal and the local track advancing after foreground recovery. All 165 `dist` assets matched native `public`; there was no development-server URL, cloud endpoints were blank, and MusicKit/purchases remained false. Evidence: `/tmp/reminduh-native-navigation-final-sync.log`, `/tmp/reminduh-native-navigation-final.log`, `/tmp/reminduh-native-navigation-final.xcresult` and the ten screenshots in `/tmp/reminduh-native-navigation-final-shots/manifest.json`. The reusable TestFlight skill now points to these native checks and passed validation again. No new archive or upload was made.

The next increment below closes the then-outstanding simulator gaps: scheduled medication alert taps, the bonsai Rain/Breeze interaction and the Make a melody activity. Earlier native notification evidence used a test alert, and earlier bonsai evidence covered touch/pause/exit. Full VoiceOver navigation, the full Dynamic Type/device range, physical iPhone audio routes/interruptions, Focus and an actual TestFlight update remain external validation. These limits are not implied passes from the browser gate.

## Sensory access and scheduled medication alerts

A still room exposed a real missing interaction: its Garden button changed Blobby's state without opening the bonsai. It now opens the existing sensory garden and keeps that session open until an explicit exit, rather than ending on the old 16-second timer. The matching domain check now covers explicit completion in both room modes. Safari also needs an explicit focus target when a pointer opens a room activity; closing the activity now returns to that button. Medication/default sadness logic and all persistent schemas are unchanged.

The record sleeve's printed lettering was overflowing its fixed artwork at 200% text. Its decorative, hidden-from-accessibility text stays fitted to the illustration; actual instructions, labels and controls still enlarge. The pre-fix image is `/tmp/reminduh-melody-art-before.png`.

The real scheduled-notification test passed **1 test, 0 failures in 121.253 seconds**. It created a synthetic medication through the native form, selected a future time with the native picker, received the actual scheduled iOS alert in the background and tapped it from SpringBoard. Today’s check-ins were visible on arrival; the dose was still unrecorded and supply remained 30. The test archived only its new QA medication and restored the prior reminder setting. It did not use the Test notification action, fabricate a push payload or alter other medication data. Evidence: `/tmp/reminduh-native-scheduled-alert-selector.xcresult`, its log and `/tmp/reminduh-native-scheduled-alert-shots/manifest.json`. A first test build had a Swift local-name collision; the next discovery attempt ran zero tests and is excluded. The successful run used the XCTest selector without trailing parentheses.

The packaged bonsai and melody acceptance run passed **2 tests, 0 failures in 165.909 seconds** (bonsai 90.325; melody 75.585). Native rain and breeze screenshots show the wet leaves and displaced airborne beads, while the browser metrics separately measure falling drops, detached water, moisture reduction and canopy movement. The native garden also cancels a shower on backgrounding, opens with a still room, remains open past the previous timeout and supports pause/resume/exit. The melody case completes all three untimed patterns, retains progress after a wrong note, cancels pattern playback on backgrounding, supports replay and restores the prior table and sound settings. It uses only earned/starter virtual leaves on the dedicated QA save when Vinyl corner is not yet owned. Medication history and supply remain unchanged. Evidence: `/tmp/reminduh-native-sensory-games.xcresult`, its log and four reviewed images in `/tmp/reminduh-native-sensory-games-shots/manifest.json`.

The three related browser suites passed on this change: compact/live room controls, the dated medication CTA/default sadness and optional routine activity isolation. Evidence: `/tmp/reminduh-sensory-related-browser.log`. `check:release` passed with all 97 domain cases after updating the former timed still-bonsai expectation (`/tmp/reminduh-sensory-release-final.log`). The small decorative-lettering fix followed that gate and received a fresh TypeScript/offline build (`/tmp/reminduh-sensory-art-build.log`). The game acceptance run above predates only that decorative CSS correction; its normal-size rendering is unchanged.

The offline simulator sync matched all 165 web files and retained blank cloud endpoints, no development-server URL and disabled MusicKit/purchase gates. No archive, TestFlight upload, backend migration or deployment is part of this increment. Physical speaker output/audio routes and interruptions, Focus/device restart notification delivery, an actual installed TestFlight update and full VoiceOver/Dynamic Type device coverage remain external validation.

The final sensory browser suite passes all 12 scenarios across Chromium and WebKit (`/tmp/reminduh-sensory-games-acknowledged.log`, `rebuild/generated/refinement/sensory-games/results.json`). It verifies actual canvas wetness/motion and decoded melody sound, wrong-note recovery, all rounds by keyboard, focus restoration, an 18-second still-room stay, 320px/landscape/200% layouts and unchanged health/routine/reward data. The artwork now fits while measured instruction text doubles. The new suite is included in the 22-script release browser runner; the earlier complete runner result covered 21 scripts, and the added/affected suites were run separately on this source. The test driver now waits for each visible note acknowledgement and the next round's promised focus before sending another key; its earlier rapid-focus failure is retained in `/tmp/reminduh-sensory-games-verified.log`. An earlier new assertion assumed a 15px base caption at every width; the corrected check measures the actual responsive base before testing enlargement.

The final native melody flow also passes at accessibility-large: **1 test, 0 failures in 103.809 seconds** on the package including the artwork correction. All three patterns, playback interruption, replay and final controls remain usable with enlarged text. The first run rejected Next melody because its test-only bottom margin was 40 points while the dedicated QA iPhone's home-indicator inset is 34; its button was fully visible at y774–840. The driver now uses the observed inset. Both failed and passing result bundles remain available: `/tmp/reminduh-native-melody-accessibility.xcresult` and `/tmp/reminduh-native-melody-accessibility-final.xcresult`, with logs. The final screenshot in `/tmp/reminduh-native-melody-accessibility-final-shots/manifest.json` confirms the larger labels and reachable completion actions. The QA system text category was restored to normal `large`.

The final compiled simulator app and native source package each match all 165 `dist` files. The built plist retains bundle `com.reminduh.app`, 0.1.0 (4), false MusicKit/purchase flags, no server URL and no configured cloud endpoints in compiled JavaScript. This is local build-4-numbered simulator code, not a replacement of Apple's immutable distributed build 4. Native copy/build logs are `/tmp/reminduh-native-sensory-art-sync.log` and `/tmp/reminduh-native-melody-accessibility-final.log`.


## CI activity focus follow-up

The first draft-PR CI run ([34527224572](https://github.com/finnerzbtz/medbuddy/actions/runs/34527224572)) passed repository/contracts/assets/build but failed WebKit's mobile sand-exit focus assertion. The same unchanged test passed locally; inspection found two competing focus owners: RoutineActivity scheduled from the close event, while its child dialog could restore focus later in passive cleanup. The activity now schedules its final restoration from unmount cleanup, after its child dialogs. It preserves focus during navigation or another open modal and falls back to the For you heading if recording removes the original button. No persistent state or medication logic changed.

The focused local rerun passed WebKit/Chromium mobile sound, sand, safe-area, rotation and focus checks, plus all five routine activity entry/replay/cancel paths and explicit save/fallback focus (`/tmp/reminduh-mobile-focus-fixed.log`). The mobile assertion now also checks that focus remains after another rendering interval and logs the active element on failure. `check:release` passed again, including 97 domain cases and a fresh offline build (`/tmp/reminduh-focus-release-check.log`). This is a timing correction supported by code inspection and passing local regression checks; the original CI failure is retained rather than described as a local reproduction. Diagnostic screenshots remain local; the workflow does not upload generated test data.


The packaged focus follow-up passed the native optional routine create/activity/pause/resume/Done/relaunch flow: **1 test, 0 failures in 87.794 seconds**, with medication history and supply unchanged (`/tmp/reminduh-native-focus-final.xcresult`, its log and four reviewed images in `/tmp/reminduh-native-focus-final-shots/manifest.json`). Its native source and compiled app matched all 165 web assets; offline integration gates remained disabled.

The second CI run ([34527893612](https://github.com/finnerzbtz/medbuddy/actions/runs/34527893612)) passed the corrected mobile focus test, all audio/voice suites and Sound, then found the bonsai still in its approach phase after 18 seconds on the slow renderer. Both Character and RoomJourney truncated every frame to 0.05 seconds. At low frame rates that slowed an analytically sampled route instead of simply drawing fewer frames. RoomJourney now consumes finite positive active time, while numerical visual smoothing retains its existing bound. Character discards the first delta on activity/playing changes using a layout effect, excluding demand-loop and background pauses. Arrival remains tied to the actual plant position; no timeout bypass opens the garden early.

The new frame-rate regression failed before the correction and passed afterwards at 1, 2, 15 and 60 FPS for garden, tea, feeding and rest. All **98 domain cases** and the full repository/contracts/assets/offline build check passed (`/tmp/reminduh-journey-low-fps-before.log`, `/tmp/reminduh-journey-low-fps-fixed.log`, `/tmp/reminduh-low-fps-release-check.log`). The expanded sensory browser suite passed **14 scenarios across Chromium and WebKit**, including deliberately delayed animation frames, actual travel/arrival position, rain/breeze, background handling, still-room persistence, melody audio/rounds, reflow/focus and health-data isolation (`/tmp/reminduh-sensory-low-fps-browser.log`). The correction changes animation timing only; schemas, medication/default-sadness logic and release/integration gates are unchanged.


The updated native garden package passed **1 test, 0 failures in 88.709 seconds**, covering rain/breeze, pause/background handling, still-room access and medication isolation (`/tmp/reminduh-native-low-fps-garden.xcresult` and log). Both packaged web copies matched all 165 built assets. A separate four-case Chromium/WebKit probe confirmed a 1.5-second pause/hidden interval leaves journey elapsed time unchanged and resumes by a normal frame, with preferences and saved content restored (`/tmp/reminduh-journey-pause-resume-final.log`; reproducible helper `/tmp/reminduh-journey-pause-resume.mjs`). Its first content assertion included the legitimate preference-update timestamp and is retained as a test-driver failure in `/tmp/reminduh-journey-pause-resume.log`; the corrected comparison excludes only updatedAt.

The third CI run ([34529030432](https://github.com/finnerzbtz/medbuddy/actions/runs/34529030432)) passed the slow-rendered garden approach and all interaction/round checks in both engines, then found decorative record lettering wider than its cover with Linux WebKit's fallback font (137 versus 122 pixels). The cover now uses an aria-hidden inline SVG with explicit text lengths; actual instructions and controls still enlarge. The first SVG geometry check caught insufficient ascent space, so the final viewBox includes vertical and horizontal margins. The check measures each text element's real glyph bounds rather than relying on SVG scroll dimensions, which can conceal clipping.

The final local sensory suite again passed all **14 scenarios** in both engines (`/tmp/reminduh-sensory-artwork-inset.log`); the earlier insufficient-ascent attempt remains in `/tmp/reminduh-sensory-artwork-portable.log`. The enlarged WebKit cover screenshot was reviewed. `check:release` passed with 98 domain cases and a fresh offline build (`/tmp/reminduh-artwork-release-check.log`). The 22-suite release runner now collects independent suite failures and fails at the end if any failed, so one run reports all outstanding failures. It does not suppress errors, skip suites, change the integration gates or upload diagnostic artifacts.


## Enlarged text and platform font wrapping

The fourth CI run ([34530429467](https://github.com/finnerzbtz/medbuddy/actions/runs/34530429467)) completed all 22 browser suites: 21 passed, while the typography suite found five route overflows in Linux WebKit at 320px and 200% text. Page headings retained their flex minimum width, outfit labels outgrew their cards, and the profile Help link still inherited an older no-wrap rule.

Headings and profile links now shrink and wrap within their container. Outfit columns adapt to text size, and their labels wrap while preserving the selected icon. Medication status and voice-preview controls move to another line when necessary, leaving more room for medication names and voice descriptions. Text retains the user's selected size; no overflow is hidden. This is layout-only and leaves medication data, optional routines, reminders and release gates unchanged.

The local 40-case typography suite passed in Chromium and WebKit at 320px and 390px with doubled actual text. Reviewed screenshots confirm that voice descriptions and previews no longer overlap. The full repository/contracts/assets/TypeScript/offline build check also passed, including all 98 domain cases. The next GitHub run verifies the same layouts with Linux platform fonts.


The fifth CI run ([34531894711](https://github.com/finnerzbtz/medbuddy/actions/runs/34531894711)) passed all other suites and 39 of the 40 typography cases. The remaining add-medication case had 336px of page width with no oversized element box. A local reproduction using DejaVu Sans regular and bold faces identified the actual text: the enlarged “The essentials” heading painted to x335.94 while its block stayed inside the form. Allowing that section heading to wrap brought page width back to 320px in both engines, without clipping or shrinking the text. System, DejaVu and Liberation font probes all fit after this targeted correction.


## Preserve entry during delayed navigation

The sixth CI run ([34533051168](https://github.com/finnerzbtz/medbuddy/actions/runs/34533051168)) passed all 40 typography cases and the other layout/activity checks, but encountered an intermittent medication-save navigation failure. An unchanged local WebKit run reproduced an empty medication name with the other fields valid; native form validation correctly prevented the save. A controlled delayed-frame regression then demonstrated that AppShell's navigation callback could move focus away from a field after entry had begun.

The default navigation callback now preserves an active editable control inside the new page before scrolling or moving focus. The pathname-keyed main prevents an old page's field from suppressing navigation. Explicit section/feed targets keep their prior behavior, and medication validation is unchanged. The regression fails before this correction and passes afterwards in both engines, followed by the real save, arrival confirmation, later navigation, Undo and backup-restore checks. It holds and releases animation frames instead of adding a delay or retrying text entry. Concise synthetic-fixture validation details are now logged if this suite fails.

All 12 local confirmation/navigation cases and the full release check passed after the correction, including the 98 domain checks and a fresh offline build.

The related isolated checks also passed: 21 medication-autocomplete cases and eight Sound-dialog cases, including keyboard/touch entry, editing, validation focus, composition, close/escape focus and retained settings.
