# Reminduh 0.1.0 (3) — internal TestFlight beta

Archive source: `984847b94ceb418799f5625f14e0fcd79a227c55`.
Release tag: `ios/0.1.0-3`. Bundle `com.reminduh.app`, team `37N43RUU8P`.

This build repairs bundled-media loading in the Capacitor iPhone environment,
activates the native playback audio session after an audio gesture, preserves audio
preferences, and restores automatic narration after audio becomes available.
The lamp's synthesized fallback is not evidence that recorded music/voice works.
Quotes share Blobby's compact reaction bubble; settings use expandable sections.

The beta is offline, with cloud endpoints blank, purchases and Apple Music disabled.
No backend migration or medication/self-care schema change is included.

## Verification

Release contracts, medication/routine/reminder browser checks, WebKit and Chromium
recorded music/voice output, mute/interruption recovery, mobile sand layout and
accessibility checks passed. Focused tests cover trusted native status-zero media
responses, empty/failed responses, settings deep links and a single speech bubble.
TypeScript, repository hygiene, native simulator build and signed archive passed.
Strict signature validation passed using the system trust store. All 165 archived
web files match dist, and the native configuration has no live development URL.

Update in place without uninstalling. Check saved schedules, records, supply and
reminders. In Sound settings try Test sound, a voice preview and background music,
with Silent mode on/off, headphones and after lock/unlock. Physical iPhone playback,
upgrade persistence and native accessibility remain device validation.

## Distribution

Xcode Organizer confirmed upload on 10 September 2026 at approximately 16:37 London
using TestFlight Internal Only. Apple processing completed; testing notes are saved,
and Reminduh Beta lists build 3 as **Testing** for its existing tester.
App Store Connect build ID: `7af5b890-6550-4221-a785-f306b8730524`.

The release branch and annotated tag are pushed. Draft PR #1 tracks the changes;
its remote verification is running. Local verification passed as described above.
